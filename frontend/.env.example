# Base URL of the backend FastAPI service
# In local development, leave empty to use Vite's proxy (/api)
# In production (Render, Vercel, Netlify), set to your backend URL:
VITE_API_URL=https://rag-engine-ads3.onrender.com
